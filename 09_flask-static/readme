New Deal: Fang, Ryan, Anson

Predictions: 
- app.py will print "the __name__ of this module is... __main__" in the terminal and turn on "debug mode" to update the web browser
  upon refresh when file is updated
- Local web server displays "No hablo queso!" in the browser
- "Is this plaintext, though?" will be displayed in the browser when loading http://localhost:5000/static/foo.html
- Everything in foo will be displayed in the browser when loading http://localhost:5000/static/foo

Breaking Disco:
- Loading http://localhost:5000/static/foo into a browser prompts a download of the file foo onto local desktop

QCC:
- Is <!DOCTYPE html> necessary? If so, why?
